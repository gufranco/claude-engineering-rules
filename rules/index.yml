# Rule Index
#
# Two tiers: rules/ (always loaded) and standards/ (loaded on demand).
# Before domain-specific work, match the task against triggers below
# and read the relevant standards/ files.

# ── Tier 1: Always loaded (rules/) ──────────────────────────────────

always_loaded:
  code-style:
    description: DRY/SOLID/KISS, immutability, TypeScript conventions, error classification, data safety, enums, validation, maximum compiler and checker strictness
    triggers: [style, typescript, type, enum, interface, immutable, validation, zod, dto, tsconfig, strict, compiler, strictness]

  code-review:
    description: PR authoring, review comment style, test evidence, branch freshness, tech debt tracking, ADRs
    triggers: [review, pr, pull request, code review, adr, tech debt]

  debugging:
    description: Four-phase debugging process, multi-component tracing, common traps to avoid
    triggers: [debug, bug, error, trace, reproduce, isolate, root cause, stack trace]

  documentation:
    description: Preserve existing valid information when skills or automated processes modify documentation files
    triggers: [readme, docs, documentation, changelog, guide, skill output, merge]

  git-workflow:
    description: Conventional commits, branch naming, local quality gate before commit/push, CI monitoring, PR creation, conflict resolution, rollback strategy
    triggers: [git, commit, branch, merge, rebase, ci, pipeline, release, tag, lint, format, build]

  language:
    description: Response language enforcement, all output in English regardless of user input language
    triggers: [language, english, translation, locale]

  pre-flight:
    description: Pre-implementation verification gates for duplicates, architecture fit, interface verification, scope agreement
    triggers: [planning, pre-flight, scope, interface, duplicate, architecture fit]

  security:
    description: Secrets management, auth checklist, encryption, data privacy, audit logging, supply chain security
    triggers: [security, auth, encryption, secret, password, token, csrf, xss, injection, owasp, certificate, ssl, tls]

  testing:
    description: Integration-first philosophy, strict mock policy, AAA pattern, fake data generators, deterministic tests, snapshot guidelines
    triggers: [test, testing, unit, unit test, mock, coverage, jest, vitest, pytest, fixture, snapshot, e2e]

  verification:
    description: Evidence-based completion gates, fresh output required for every claim, no rounding up partial completion
    triggers: [verify, evidence, complete, done, claim, proof, gate]

  writing-precision:
    description: Precision gate for all text output, concrete over abstract, examples over vague instructions, self-test before finalizing
    triggers: [write, writing, text, prose, message, comment, description, rule, standard, precision, pr description, code comment, error message]

  github-accounts:
    description: Multi-account safety for gh CLI, require inline GH_TOKEN, block gh auth switch, account mapping by remote URL
    triggers: [github, gh, gh auth, multi-account, token]

  gitlab-accounts:
    description: Multi-account safety for glab CLI, require inline GITLAB_TOKEN and GITLAB_HOST, block glab auth login
    triggers: [gitlab, glab, glab auth, multi-account, token]

  agent-usage:
    description: Agent and parallelism budget rules, prefer inline tool calls over spawning agents, max two concurrent agents, rate budget awareness
    triggers: [agent, spawn, parallel, subagent, rate limit, rate budget, parallelize, concurrent agents]

  clean-room:
    description: Clean room implementation process to avoid plagiarism and legal exposure when using external projects as reference or inspiration
    triggers: [clean room, plagiarism, copyright, license, reference, inspiration, external source, derivative, similar, copy]

# ── Tier 2: On demand (standards/) ──────────────────────────────────

on_demand:
  api-design:
    path: standards/api-design.md
    description: REST conventions, status codes, error format, pagination, versioning, deprecation, rate limiting, bulk operations
    triggers: [api, endpoint, route, controller, rest, http, request, response, pagination, webhook]

  borrow-restore:
    path: standards/borrow-restore.md
    description: Safe global state management for CLI tools with context switching and guaranteed restore
    triggers: [docker, kubectl, terraform, gh, context, workspace, profile, switch]

  caching:
    path: standards/caching.md
    description: Cache strategies, invalidation, thundering herd prevention, warming, sizing, eviction policies
    triggers: [cache, redis, memcached, ttl, invalidation, cdn, lru, stale]

  cost-optimization:
    path: standards/cost-optimization.md
    description: Cost allocation tagging, compute right-sizing, storage lifecycle policies, network cost reduction, database cost optimization, budget alerts, FinOps practices
    triggers: [cost, finops, budget, savings plan, reserved instance, spot instance, right-sizing, tagging, cost allocation, cost optimization, lifecycle policy, unused resources]

  database:
    path: standards/database.md
    description: Schema rules, query optimization, isolation levels, safe migrations, locking, connection management, NoSQL key design
    triggers: [database, sql, migration, schema, query, index, transaction, postgres, dynamo, mongo, redis]

  distributed-systems:
    path: standards/distributed-systems.md
    description: Consistency models, saga pattern, outbox, distributed locking, event ordering, schema evolution, zero-downtime deploys
    triggers: [distributed, microservice, saga, event, consistency, replication, partition, outbox, cqrs]

  frontend:
    path: standards/frontend.md
    description: Typography, spacing, WCAG AA contrast, responsive design, accessibility, Tailwind, component patterns, performance
    triggers: [frontend, react, css, tailwind, accessibility, responsive, component, ui, ux, a11y, wcag]

  infrastructure:
    path: standards/infrastructure.md
    description: IaC, networking, container orchestration, CI/CD pipeline design, cloud architecture, DORA metrics, cost allocation
    triggers: [infrastructure, terraform, kubernetes, docker, aws, gcp, cloud, deploy, ci, cd, helm]

  llm-docs:
    path: standards/llm-docs.md
    description: LLM-optimized documentation URLs for 64 technologies, fetch before coding, verify APIs against docs
    triggers: [docs, documentation, api reference, library, framework, llms.txt]

  observability:
    path: standards/observability.md
    description: Structured logging, metrics naming, distributed tracing, health checks, SLIs/SLOs, alerting, incident response
    triggers: [logging, metrics, tracing, monitoring, alert, slo, sli, incident, health check, datadog, prometheus]

  resilience:
    path: standards/resilience.md
    description: Error classification, retries with backoff, idempotency, deduplication, DLQs, circuit breakers, back pressure, bulkheads
    triggers: [retry, idempotent, circuit breaker, dlq, backpressure, timeout, rate limit, resilience, fault tolerance]

  identifiers:
    path: standards/identifiers.md
    description: Identifier type selection guide covering UUID v1-v8, ULID, TSID, Snowflake, Sonyflake, Randflake, TypeID, CUID2, NanoID, xid, Sqids, and auto-increment. Decision flowchart, use-case matrix, database storage, index performance
    triggers: [identifier, id, uuid, ulid, tsid, snowflake, sonyflake, cuid, nanoid, typeid, primary key, pk, serial, auto-increment, bigserial, objectid, sqids, hashids, randflake, xid, overflow, partition key, shard key]

  twelve-factor:
    path: standards/twelve-factor.md
    description: Cloud-native app design across all 12 factors with actionable rules for stateless processes, config, backing services, disposability, and admin processes
    triggers: [twelve-factor, 12-factor, cloud-native, saas, stateless, port binding, backing service, disposability, admin process, dev-prod parity]

  hexagonal-architecture:
    path: standards/hexagonal-architecture.md
    description: Ports and adapters pattern with dependency direction, port interfaces, adapter implementations, and per-layer testing strategy
    triggers: [hexagonal, ports and adapters, port, adapter, dependency direction, architecture layers, domain isolation, layered architecture]

  railway-oriented-programming:
    path: standards/railway-oriented-programming.md
    description: Result type composition with map, flatMap, error accumulation, pipe/flow, neverthrow, Effect, and boundary conversion
    triggers: [railway, result type, neverthrow, effect, andThen, flatMap, error accumulation, result chaining, functional error, typed errors, result monad]

  ddd-tactical-patterns:
    path: standards/ddd-tactical-patterns.md
    description: Entities, value objects, aggregates, domain events, repositories, domain services, and ubiquitous language
    triggers: [ddd, domain-driven, entity, value object, aggregate, domain event, repository pattern, ubiquitous language, bounded context]

  state-machines:
    path: standards/state-machines.md
    description: Type state pattern, runtime state machines, transition tables, guard conditions, XState, and testing strategies for stateful workflows
    triggers: [state machine, state chart, statechart, xstate, transition, workflow, lifecycle, type state, guard condition]

  accessibility-testing:
    path: standards/accessibility-testing.md
    description: WCAG AA/AAA compliance, axe-core integration, Lighthouse CI, Pa11y, manual testing checklist, ARIA patterns, focus management, color contrast verification, screen reader testing
    triggers: [accessibility, a11y, wcag, axe, axe-core, jest-axe, playwright-axe, cypress-axe, lighthouse, pa11y, screen reader, voiceover, nvda, talkback, aria, focus management, contrast, keyboard navigation]

  algorithmic-complexity:
    path: standards/algorithmic-complexity.md
    description: Data structure selection guide, sorting algorithm comparison, complexity analysis rules, common anti-patterns with hidden costs, space complexity awareness
    triggers: [performance, big-o, complexity, algorithm, data structure, sort, hash, heap, tree, map, set, queue, O(n), quadratic, optimization, memory, space complexity, oom, memory leak, unbounded growth, scaling issue]

  multi-tenancy:
    path: standards/multi-tenancy.md
    description: Tenant isolation strategies, noisy neighbor prevention, data leakage prevention with RLS, tenant-scoped observability, tenant lifecycle, cross-tenant testing, per-tenant configuration
    triggers: [multi-tenant, tenant, tenancy, tenant_id, isolation, noisy neighbor, rls, row-level security, tenant context, tenant onboarding, tenant offboarding, per-tenant]

  graphql-api-design:
    path: standards/graphql-api-design.md
    description: GraphQL schema design, query complexity limits, N+1 prevention with DataLoader, error handling with union types, field-level authorization, federation, subscriptions, persisted queries, schema evolution and deprecation lifecycle
    triggers: [graphql, schema, resolver, mutation, subscription, dataloader, federation, supergraph, subgraph, apollo, relay, connection, edges, persisted query, query complexity, introspection]

  websocket-realtime:
    path: standards/websocket-realtime.md
    description: WebSocket and SSE lifecycle, reconnection with backoff, message envelopes, sequence ordering, deduplication, backpressure, horizontal scaling with pub/sub, security hardening
    triggers: [websocket, ws, sse, server-sent events, real-time, realtime, socket, pub/sub, heartbeat, ping, pong, reconnect, bidirectional]

  terraform-testing:
    path: standards/terraform-testing.md
    description: Terraform built-in test framework with .tftest.hcl files, plan vs apply mode, mock providers, parallel execution, expect_failures, state key isolation
    triggers: [terraform test, tftest, .tftest.hcl, terraform mock, terraform plan test, terraform integration test, terraform unit test, hcl test]

  feature-flags:
    path: standards/feature-flags.md
    description: Flag types and lifecycle, evaluation performance with local cache, rollout strategies with automatic rollback, flag cleanup and expiration, testing both states, boundary evaluation pattern, observability, platform comparison
    triggers: [feature flag, feature toggle, flag, toggle, rollout, canary, a/b test, experiment, kill switch, launchdarkly, unleash, flagsmith, growthbook, percentage rollout, flag cleanup]

  message-queues:
    path: standards/message-queues.md
    description: Topology patterns, message envelope format, consumer patterns, ordering guarantees, DLQ and error handling, backpressure, exactly-once processing, monitoring, testing, platform comparison
    triggers: [queue, message queue, broker, sqs, rabbitmq, kafka, nats, redis streams, consumer, producer, publisher, subscriber, dlq, dead letter, pub/sub, topic, fanout, outbox, deduplication, consumer lag, prefetch]

  browser-testing:
    path: standards/browser-testing.md
    description: Playwright test architecture, page objects, visual regression, accessibility tree testing, responsive testing, Core Web Vitals measurement, cookie management, CI configuration
    triggers: [browser, playwright, e2e, visual regression, screenshot, viewport, responsive, core web vitals, lighthouse, cwv, lcp, cls, inp, page object, axe, browser test]

  grpc-services:
    path: standards/grpc-services.md
    description: gRPC service design, proto file conventions, error handling with status codes, deadlines, interceptors, health checking, reflection, load balancing for HTTP/2
    triggers: [grpc, protobuf, proto, streaming, reflection, grpc-web, grpc health, interceptor, deadline, status code]

  i18n-l10n:
    path: standards/i18n-l10n.md
    description: Text externalization, pluralization with ICU MessageFormat, number/date/currency formatting, RTL support, locale detection, translation workflow, pseudo-localization testing
    triggers: [i18n, l10n, locale, translation, rtl, pluralization, intl, internationalization, localization, icu, message format, right-to-left]

  data-pipelines:
    path: standards/data-pipelines.md
    description: Batch, micro-batch, and streaming pipelines, idempotent processing, backfill strategy, data quality validation, error handling, ETL vs ELT
    triggers: [etl, elt, pipeline, batch, streaming, backfill, data quality, data pipeline, ingestion, transformation, data warehouse]

  mlops:
    path: standards/mlops.md
    description: Model lifecycle, experiment tracking, feature stores, model serving patterns, data and concept drift detection, training/serving parity testing
    triggers: [ml, model, training, inference, feature store, drift, mlflow, experiment, mlops, machine learning, serving, prediction]

  ab-testing:
    path: standards/ab-testing.md
    description: Experiment design, statistical rigor, power analysis, assignment and randomization, multivariate testing, analysis methodology, post-experiment documentation
    triggers: [ab test, a/b test, experiment, multivariate, statistical significance, power analysis, p-value, conversion, variant, control group, treatment]

  mobile-development:
    path: standards/mobile-development.md
    description: Framework selection, navigation, offline support, performance optimization, push notifications, app store compliance, device matrix testing
    triggers: [mobile, ios, android, react native, flutter, swift, kotlin, app store, push notification, offline, deep link]

  dashboard-design:
    path: standards/dashboard-design.md
    description: Purpose-driven dashboards, layout principles, metric grouping with RED method, SLO visualization with error budgets, chart selection, alert integration
    triggers: [dashboard, grafana, metric grouping, slo visualization, error budget, burn rate, panel, chart, monitoring dashboard]

  documentation-generation:
    path: standards/documentation-generation.md
    description: OpenAPI generation from code, AsyncAPI for events, GraphQL schema docs, TypeDoc, documentation-as-code, versioning, CI validation
    triggers: [openapi, asyncapi, swagger, typedoc, docs generation, api docs, schema docs, docusaurus, mkdocs, documentation site]
